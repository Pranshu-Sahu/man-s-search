---
// PageHeader.astro — Cursive page number with tildes
interface Props {
  pageNumber: number;
}

const { pageNumber } = Astro.props;
---

<header class="page-header">
  <span class="page-number">~ {pageNumber} ~</span>
</header>
